#!/bin/bash
sudo su
#
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.34.0/install.sh | bash
#
. ~/.nvm/nvm.sh
# Instalamos node
nvm install node

# Instalamos angular 
npm install -g @angular/cli
